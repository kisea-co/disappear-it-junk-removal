import EstimateForm from './EstimateForm';

const rewardBanner = {display:'inline-flex',alignItems:'center',gap:'10px',margin:'22px 0 8px',padding:'13px 15px',background:'#0b0b0b',color:'#f4f0e8',borderLeft:'4px solid var(--gold)'} as const;

export default function ContactPage({searchParams}:{searchParams?:{trashketball?:string;type?:string}}){
const gameReward:0|25|50 = searchParams?.trashketball === '50' ? 50 : searchParams?.trashketball === '25' ? 25 : 0;
const estimateType:'residential'|'commercial' = searchParams?.type === 'commercial' ? 'commercial' : 'residential';
return <>
<section className="page-hero"><div className="container"><div className="eyebrow">Get an Estimate</div><h1>{estimateType === 'commercial' ? <>LET&apos;S SCOPE<br/><span>THE PROJECT.</span></> : <>WHAT DO YOU NEED<br/><span>GONE?</span></>}</h1><p>{estimateType === 'commercial' ? 'Commercial cleanouts, property support and recurring service.' : 'Send the details. We’ll take it from there.'}</p></div></section>
<section className="section section-cream"><div className="container"><nav aria-label="Choose estimate type" style={{display:'grid',gridTemplateColumns:'repeat(2,minmax(0,260px))',gap:'10px',marginBottom:'42px'}}><a className={`btn ${estimateType === 'residential' ? 'btn-dark' : ''}`} href={`/contact${gameReward ? `?trashketball=${gameReward}` : ''}`}>Residential Estimate</a><a className={`btn ${estimateType === 'commercial' ? 'btn-dark' : ''}`} href={`/contact?type=commercial${gameReward ? `&trashketball=${gameReward}` : ''}`}>Commercial Estimate</a></nav><div className="editorial-split"><div><div className="eyebrow dark">{estimateType === 'commercial' ? 'Commercial Intake' : 'Quick Residential Estimate'}</div><h2>{estimateType === 'commercial' ? <>TELL US ABOUT<br/>THE PROPERTY.</> : <>SHOW US<br/>THE JUNK.</>}</h2>{gameReward > 0 && <div style={rewardBanner}><small>TRASHKETBALL REWARD</small><strong style={{fontSize:'1.7rem',lineHeight:1,color:'var(--gold-soft)'}}>${gameReward} OFF</strong><span>ANY LOAD SIZE</span></div>}<p className="section-lead">{estimateType === 'commercial' ? 'Share the site, project scope and scheduling needs. We’ll review the details and follow up to confirm access, availability and commercial pricing.' : 'Tell us where the job is and what needs to be removed. We’ll review the details, confirm availability and contact you with your free estimate.'}</p><div className="hero-actions"><a className="btn btn-dark" href="tel:+14708701067">Call Us</a></div></div><div><EstimateForm reward={gameReward} estimateType={estimateType}/></div></div></div></section>
</>}
